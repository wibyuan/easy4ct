# AGENTS.md

## 项目概述

easy4ct：基于启发式搜索的四色定理多样性证书开源生成器——"四色异迹"系统（本科生研究项目）。

当前阶段：资料整理、文章撰写（`doc/res.md`）、RSST 1997 验证代码的溯源与复现。

## 目录约定

- `doc/`：开源后保留的公开文档（`res.md` 文章主产物、`doc/papers/` 四篇公版论文 OCR：Kempe 1879 / Tait 1880 / Heawood 1890 / Birkhoff 1913）。
- `doc/figures/`：文章插图统一存放处。文件名须无空格（Obsidian 粘贴产生的 `Pasted image 2026*.png` 须重命名为 `image-2026*.png` 再入库）；文章内引用统一为相对路径 `![...](figures/xxx.png)`。
- `docs/`：内部文档（项目申请书、个人信息、RSST 1997 OCR——Elsevier 版权，不可分发），已被 `.gitignore` 忽略、不进入版本控制；开源前须整体删除。
- `DEVLOG.md`：项目开发日志（关键决策与进展，随时间线追加）。
- `rsst/`：RSST 1997 验证代码（学术用途转载，见其 README）。
- `fourcolor/`：Gonthier Coq 形式化证明 submodule（CeCILL-B 许可）。
- `README.md` 记录完整约定，修改目录结构时同步更新。

## 协作规则（用户明确要求，必须遵守）

0. **未经用户明确授权，禁止修改或提交 `res.md` 及任何用户创作中的文档**（2026-08-10 用户禁令：因助手未经确认擅自用脚本改动 `res.md` 导致误删图片行，被永久收回修改文档的权限，仅此一次写入本禁令）。
1. **逐步确认制**：每一步先盘问细节、与用户达成一致再动手；禁止自动推进整个任务链。
2. **理清决策依赖**：先做前置决策（如：先定下载源 → 再定目录结构 → 再谈实现），后做后置决策；不要越级推进。
3. **先查库再问**：能从仓库已有代码/文本（`doc/`、`docs/` 的 OCR、`res.md`）回答的问题，先探索再提问。
4. **每个问题必带推荐答案**：不允许把决策纯抛给用户。
5. **细粒度 commit**：单次修改独立提交，禁止把多个不同修改合并为一个 commit。
6. **一次只问一个问题**。
7. **用户 abort 是停止信号**：用户中止命令后，立即停止当前动作，总结汇报，等待指示。
8. **提问不动文件**：用户在提问、讨论阶段（如"还需要吗""要不要改"等），禁止修改任何文件；先回答问题、与用户达成一致后再动手。
9. **全文读完才开口**：涉及文章内容的任何建议、提问、评估前，必须完整读完相关文件（`res.md` 或对应拆分文件、`docs/` 的 RSST OCR、DEVLOG 等）全文，并声明"已读：xxx 全部"；禁止只读片段就以为了解全文（2026-08-12 教训：未读 res-02/04/05/06/08 全文就问"是否引入三染色"，文中可能有答案）。
10. **提问自查**：提问前先过"该问题是否能在仓库文本中找到答案"——能找到就不问，查完再问。
11. **建议分级**：基于原文（可标注出处行号）的建议才有权重；基于一般知识的推测默认作废、不说出口，除非用户明确要求，且必须声明"这是常识推测、未经原文核对"。
12. **引用层面与表述框架分离**：转述来源论文时的处理策略（如 RSST (4.1) 疏漏的引用方式、是否引 Gonthier 佐证）属于引用层面；文章自身的表述框架（是否采用 hypermap、γ 记账等）由作者决定——两者不得混淆（2026-08-12 教训："走 Gonthier 式表述"把引用佐证说成行文框架，后者不可能是科普行文语言）。
13. **拿不出依据就闭嘴**：宁可少说，不说猜的。
14. **起草申请前置阅读**：申请起草任何章节内容前，必须先完成并声明"已读"：来源论文对应部分全文（如 RSST §2-3 正文 OCR）、文章全部前文（res-01 至目标章节）。未完成并声明之前，禁止提出起草申请或动笔——先读、声明、再申请（2026-08-12 教训：申请起草时未读 RSST、未读前文，产出天降公理废稿）。
15. **写作对照**：起草时每个概念、定义、公理、结论必须逐条对照前文与来源论文原文（可标注前文出处或 OCR 行号），禁止凭记忆转述；凭记忆输出的内容不合格。
16. **可重写标准**：续写/起草产出底线是作者可在架构上重写——概念引入有来历、机制与衔接正确、来源标注清晰；作者须全删重来即废稿，不得交付。
17. **概念来历检查**：引入新概念/定义/公理前自查——前文是否已有类似概念（有则沿用其语言与术语）、读者是否会问"这从哪来的"、能否用文章已有术语体系表达（不能则说明表述框架被来源论文带跑）。
18. **项目结论核对**：来源论文的每条定义、公理、结论写入文章前，必须逐一核对项目已决结论（DEVLOG、分析文档、README），已知有疑点的条款（如 RSST 环尺寸条款对正二十面体）必须显式处理（注明例外或引佐证），未核对不得写入。

## 脱敏纪律（开源前强制）

- `docs/`、申请材料、个人身份信息（姓名、学号、手机、邮箱、学校与项目计划关联）不得出现在任何已跟踪文件中。
- 历史中若存在敏感内容，未推送时直接重写本地历史；已推送需 `git filter-repo` 清历史后再 force push。

## 文章格式规范（撰写文章/文档时遵守）

参考样例：`doc/format-example.md`（知乎《环》一文，完整展示了以下格式）。

文章是作者自己的严谨证明文章（科普定位，"严谨到死"标准），不是任何来源论文的通俗化改写：来源（RSST、Birkhoff、Gonthier 等）是引用与归属的对象，不是行文脚本；是否采用或如何呈现来源的字面表述（如 RSST 的 γ 记账与引理 (4.1) 的字面）由作者决定。

1. **定义/定理用引用块**：`> **定义（名称）**` 或 `> **定理（名称）**` 开头，条目名加粗，内容为引用块内若干行。**引用块内不得使用列表结构（无序/有序列表均不允许）**；块内即使有多个性质，也应当以连贯语句相互联系，不能用列表偷懒取代联系。**定义的行为判据**：任何向文章引入新术语（词或短语）的语句，无论句式（"称……为……""……称为……""……叫做……""记……为……"等）与出现形式（引用块、行内、括号、普通段落、是否加粗），一律构成定义，必须使用引用块；是否加粗不是判据，未加粗的命名同样违规。
2. **证明起止标记**：以加粗 `**证明：**` 开始，`**证明：**` 之后换行留空行再接证明内容；以黑色方块 `$\blacksquare$` 结束，`$\blacksquare$` 紧跟证明最后一句之后（同行），且其后换行留空行再接后续内容。多情形证明可用 `**证明1（xxx）：**`、`**证明2（xxx）：**` 开始，同行直接接证明内容，各自以 `$\blacksquare$` 结束。**伪证以加粗 `**伪证：**` 开始（其后换行留空行接内容），以 `$\blacksquare$` 结束，与正常证明区分**。**证明块内不得出现任何定义（包括但不限于行内命名语句），也不得嵌套任何引用块或证明块**；引用块和证明块须平级排列。**定理与其对应的证明块之间不得出现任何定义、引用块或其它证明块**。
3. **公式格式**：公式 `$...$` 内部不得有首尾空格，`$` 与内容必须直接贴合（如 `$G_1$`，不能是 `$G_1 $` 或 `$ G_1$`）。
4. **图注**：图片后允许空行再接图注，解释绘图的基本意义，以"如图"为提示起始语（暂不编号，合并时统一编号）。图注不加数学公式渲染（避免喧宾夺主、取代正文），以纯文本辅助理解为主，严谨性由正文保证；后续转 LaTeX 时可按需补充公式渲染。
5. **暂不加引用、总结、概括或导语**：撰写完成后再统一分配。
6. **长文拆分**：markdown 过长时拆分为多个文件（按文章章节），便于人类编辑；最终转 LaTeX + TikZ PDF 时再合并。
7. **禁止简化**：文章以"严谨到死的科普"为标准——所有术语必须严格定义（不得以通俗为由省略、替换或自造替代词）；所有定理必须给出完整证明（不得跳过、不得以"众所周知"或引用带过）；表述不得使用模糊的日常词（如未定义的"包围"）；内容不得以"科普不需要""与科普前文不合"为由裁剪；转述来源论文（RSST、Birkhoff 等）中的定理、定义与结论时须与原文一致、不得歪曲归属——该约束仅及于转述来源内容，不约束文章自身的表述框架与结构选择。
8. **定义最小化**：定义只含判定条件（公式、结构要求），不含任何需要证明才成立的性质——性质一律归定理；定义里出现"需证明才成立的条款"即公理，禁止。
9. **命题二选一**：同一命题不得既在定义中被假设、又在定理中被证明。定理能证明的，从定义中删除；定义保留的，不得再作定理结论。
10. **术语唯一**：每个术语只定义一次。由定理确立的术语（如帽顶点/扇顶点），定义中不得先期按另一含义使用。
11. **量词闭合**：定理写"每个 $X$……"时，证明必须覆盖全部 $X$——对象覆盖的桥（如集合等式）必须显式写出并被证明，不得断言。
12. **禁止省略式引用**：两个情形的论证相同时可交叉引用，但必须指明被引用的完整论证的位置，不得以"不再重复"等省略。
13. **先分工后动笔**：撰写/重写章节前，先列"定义-定理分工清单"：定义承担什么（最小谓词）、每条定理承担什么（存在/结构/唯一）、术语在哪引入——清单确认后再写。
14. **重写即重新设计**：修改既有文本时先检查原结构的定义/定理边界，发现"定义带公理"类结构问题必须重构，禁止最小改动式修补。
15. **定义自查三问**：定义写完自查——(a) 有无"需证明才成立"的条款？(b) 有无术语重复定义？(c) 每条全称定理的覆盖桥是否已证明？

## RSST 代码溯源状态

- 论文 OCR 中记录的原始地址（1997）：`http://www.math.gatech.edu/~thomas/FC/ftpinfo.html` —— 返回 301 重定向至 https 同路径（此前 webfetch 误报 404，实为 https 路径缺失）。
- 现行地址（2017 更新）：`https://people.math.gatech.edu/~thomas/FC/fourcolor.html`（301 → `https://thomas.math.gatech.edu/FC/fourcolor.html`）
  - 程序与数据目录：`http://people.math.gatech.edu/~thomas/OLDFTP/four/`（目录列表 403，需已知文件名直连）
  - 综述 PDF：`http://people.math.gatech.edu/~thomas/OLDFTP/fcdir/npfc.pdf`、`unavoidable.pdf`
- 分工：用户负责逐个下载文件到仓库；助手负责定位、验证连通性、跑通与讲解代码（下载、构建、验证已于 2026-08-09 全部完成，详见 DEVLOG；构建产物不入库）。
- 已决决策（2026-08-09）：① 下载源——`thomas.math.gatech.edu/FC/ftpinfo.html` 官方清单页；② 落盘——`rsst/code/` + `rsst/docs/`；③ 构建——`gcc -std=gnu90 -O2`（不改动源码），产物不入库。

## 环境备注

- WSL（Linux）+ Windows 并存；Windows 侧有 PowerShell + Word/Excel COM（`/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe`），用于转换 `.doc/.xls` 等 Office 文件。
- Linux 侧 LibreOffice 无中文字体，转换中文 Office 文件请走 Windows COM 路线。
